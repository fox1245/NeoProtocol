# Editor
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Node
node_modules/
yarn.lock
pnpm-lock.yaml
# Lockfile policy: keep server/package-lock.json so deps are reproducible.
# (Earlier .gitignore had it blanket-excluded; revisited at v0.2 — see commit history.)

# Python (server impl, not used yet)
__pycache__/
*.pyc
.venv/
venv/

# Logs
*.log

# Build artifacts
dist/
build/

# Local secrets — never commit
.env
.env.*
!.env.example
